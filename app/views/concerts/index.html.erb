<script>
$(document).ready(function(){
  var table = $('table#concerts').DataTable({
        "columnDefs": [
            { "visible": false, "targets": 10 }
        ],
        "order": [[ 10, 'asc' ]],
        "displayLength": 25,
        "drawCallback": function ( settings ) {
            var api = this.api();
            var rows = api.rows( {page:'current'} ).nodes();
            var last=null;

            api.column(10, {page:'current'} ).data().each( function ( group, i ) {
                if ( last !== group ) {
                    $(rows).eq( i ).before(
                        '<tr class="group"><td colspan="5">'+group+'</td></tr>'
                    );

                    last = group;
                }
            } );
        }
    } );

    // Order by the grouping
    $('#concerts tbody').on( 'click', 'tr.group', function () {
        var currentOrder = table.order()[0];
        if ( currentOrder[0] === 2 && currentOrder[1] === 'asc' ) {
            table.order( [ 10, 'desc' ] ).draw();
        }
        else {
            table.order( [ 10, 'asc' ] ).draw();
        }
    } );
})
</script>

<div class="page-header">
  <h1>Concerts</h1>
</div>
<div class="col-lg-7 col-md-6 col-sm-12 pull-left">
  <div class="panel panel-default">
    <div class="panel-heading">Search</div>
    <div class="panel-body">
      <%= search_form_for @q do |f| %>
        <div class="form-group">
          <%= f.label "Venue name" %>
          <%= f.search_field :venue_name_cont, class: "form-control" %>
        </div>
        <div class="form-group">
          <%= f.label "By status" %>
          <%= f.collection_select :status_id_eq, Status.all, :id, :name, include_blank: "All", class: "form-control" %>
        </div>
        <div class="form-inline">
          <div class="form-group">
            <%= f.label "Concerts between" %>
            <%= f.search_field :on_date_gteq, class: "form-control js-datepicker" %>
          </div>
          <div class="form-group pull-right">
            <%= f.label "and", style:"padding-right:35px;" %>
            <%= f.search_field :on_date_lteq, class: "form-control js-datepicker" %>
          </div>
        </div><br />
        <div class="form-group pull-right">
          <%= f.submit "Search", class: "btn btn-md btn-primary" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
<div class="col-lg-3 col-md-5 col-sm-12 pull-right">
  <div class="panel panel-default">
    <div class="panel-heading">Legend</div>
    <div class="panel-body">
      <div class="form-group"><i class="icon fa fa-user" style="color:green;">&nbsp;Available</i></div>
      <div class="form-group"><i class="icon fa fa-user" style="color:red;">&nbsp;You already signed to this</i></div>
      <div class="form-group"><i class="icon fa fa-user">&nbsp;Someone already sign to this</i></div>
      <div class="form-group"><i class="fa fa-times">&nbsp;You can't sign to this</i></div>
    </div>
  </div>
</div>
<div class="row col-lg-12 col-md-12 col-sm-12">
  <%= render 'concerts/list', collection: @concerts %>
</div>
<div class="col-lg-12 text-right">
  <%= will_paginate @concerts %>
</div>
