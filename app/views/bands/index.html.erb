<div class="page-header">
  <h1>Bands</h1>
</div>
<div class="row">
  <table class="table" id="bands" data-source="<%= bands_path(format: :json) %>">
    <thead>
        <th>Id</th>
        <th>Name</th>
        <th>Country</th>
        <th></th>
        <th></th>
    </thead>
    <tbody>
    </tbody>
  </table>
</div>
<div class="row" style="margin-top:15px;">
  <%= link_to new_band_path, remote: true, class: "btn btn-md btn-primary pull-right" do %>
    <i class="fa fa-plus-circle"> New band</i>
  <% end %>
</div>

<div class="modal fade" id="band-modal">
</div><!-- /.mod
